<?php

/**
 * @file
 * Hax install hooks.
 */

/**
 * Implements hook_install().
 */
function hax_install() {
  \Drupal::messenger()->addStatus(t('Default is to serve HAX / webcomponents over a CDN but if you want to use local copies of the assets or do your own build make sure you copy everything from CopyThisStuff in the hax module directory into sites/all/libraries/webcomponents/ and read the README.md.'));
}
